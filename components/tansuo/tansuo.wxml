<!--components/tansuo/tansuo.wxml-->

<view class="ts-dea-box">
    <view class="ts-dea" wx:for="{{dea}}" wx:key="key">
      <image class="bookimg" src="{{item.src}}"></image>
      <view class="bookname">{{item.name}}</view>
    </view>
  </view>
